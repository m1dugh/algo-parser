declare function test(): int

function fibo(n: int): int
    
    if n == 0
        return 0
    end

    v1 <- test()
    
    i <- 0
    v1 <- 0
    v2 <- 1
    while i < n
        buffer <- v1
        v1 <- v2
        v2 <- v2 + buffer

        i <- i + 1
    end

    return v2
end
