function fibo(int n): int
    v1 <- 0
    v2 <- 1
    i <- 0

    if n <= 0
        return 0

    while i < n
        buffer <- v1
        v1 <- v2
        v2 <- buffer + v2
        e <- "hello, world"

    return v2
end
